# Объяснение модели классификации состояний (простыми словами)

## 🎯 Что делает модель?

Модель пытается определить **ваше текущее эмоциональное состояние** на основе 6 метрик, которые вы оцениваете:
- **Valence** (валентность) — насколько позитивно/негативно вы себя чувствуете
- **Energy** (энергия) — уровень энергии
- **Tension** (напряжение) — насколько вы напряжены
- **Clarity** (ясность) — насколько ясно вы понимаете своё состояние
- **Control** (контроль) — насколько вы чувствуете контроль над ситуацией
- **Social** (социальность) — насколько вы хотите общаться

## 🔄 Как работает классификация (упрощённо)

### Шаг 1: Преобразование метрик в вектор
Ваши 6 метрик (каждая от 1 до 9) превращаются в **вектор состояния** — набор чисел, который описывает ваше состояние в многомерном пространстве.

**Пример:**
- Если вы поставили Energy = 1 (очень низко), это означает высокую усталость (fatigue)
- Если Energy = 9 (очень высоко), это означает высокую активацию (arousal)

### Шаг 2: Сравнение с "эталонными состояниями"
У нас есть **11 состояний** (grounded, exhausted, connected, down, averse, detached, engaged, pressured, capable, blocked, overloaded).

Каждое состояние имеет **центроид** — "идеальную точку" в пространстве состояний. Модель вычисляет, насколько близко ваш вектор к каждому центроиду.

**Аналогия:** Представьте карту с 11 городами. Ваше состояние — это ваше местоположение. Модель вычисляет расстояние до каждого города и выбирает ближайший.

### Шаг 3: Фильтрация по правилам (Eligibility Gates)

**Вот тут начинается самое важное!**

Недостаточно просто выбрать ближайшее состояние. Нужно проверить, **семантически ли оно правильно**.

**Пример проблемы:**
- Ваш вектор может быть ближе всего к "connected" (связанность)
- Но если у вас высокая усталость (F_high) или высокое напряжение (T_high), то "connected" **семантически неправильно**
- Потому что "connected" означает позитивное социальное состояние, а усталость/напряжение этому противоречат

**Eligibility Gates** — это правила, которые говорят: "Это состояние НЕ может быть выбрано, если..."

**Примеры правил:**
- `connected` НЕ может быть выбрано, если:
  - F_high (высокая усталость) ИЛИ
  - T_high (высокое напряжение) ИЛИ
  - Vneg (негативная валентность)
  
- `exhausted` может быть выбрано, если:
  - F_high (высокая усталость) И
  - Ar_low (низкая активация)

## ⚠️ Проблема, которую мы решали

### Проблема: "all_filtered" (всё отфильтровано)

**Что происходило:**
1. Модель вычисляла расстояния до всех 11 состояний
2. Применяла правила eligibility (strict pass) — проверяла все правила для каждого состояния
3. **В 29.57% случаев ВСЕ 11 состояний отфильтровывались!**
4. Результат: `uncertain` (неопределённость) — 100% из-за `all_filtered`

**Почему это происходило:**
- Правила были слишком строгие
- Некоторые комбинации метрик не подходили ни под одно состояние
- Например: средние значения по всем метрикам могли не соответствовать ни одному состоянию

**Пример:**
- Valence = 5 (нейтрально)
- Energy = 5 (средне)
- Tension = 5 (средне)
- Clarity = 2 (очень низко)
- Control = 5 (средне)
- Social = 5 (средне)

Такая комбинация может не пройти ни одно правило eligibility, потому что:
- Нет явной усталости → не `exhausted`
- Нет высокого напряжения → не `pressured`, не `blocked`
- Нет высокой социальности → не `connected`
- Нет высокого контроля → не `grounded`, не `capable`
- И так далее...

## ✅ Что мы исправили (EPIC R)

### Решение 1: Двухпроходная схема фильтрации

**Было:**
- Один проход (strict) → если всё отфильтровано → `uncertain`

**Стало:**
- **Проход 1 (strict):** Проверяет все правила (и требования, и блокеры)
- **Проход 2 (hard):** Если strict отфильтровал всё, проверяет только **критические блокеры** (без требований)
- **Проход 3 (final fallback):** Если hard тоже отфильтровал всё, выбирает ближайшее **macro-состояние** без правил

**Пример:**
- Strict: "connected требует S_high И НЕ допускает F_high" → отфильтровано
- Hard: "connected НЕ допускает F_high" → если F_high нет, то можно
- Final fallback: Если и hard отфильтровал всё, просто выбираем ближайшее состояние по расстоянию

### Решение 2: Логирование для диагностики

Теперь мы собираем информацию о каждом случае `all_filtered`:
- Какие baseline-метрики привели к проблеме
- Какие уровни (Vneg, T_high, F_high и т.д.)
- Почему каждое состояние было отфильтровано
- Топ-3 причины по частоте

Это помогает понять, какие правила слишком строгие.

### Решение 3: Sanity-check покрытия

Добавлена проверка: на каждом baseline-векторе должен быть **хотя бы 1 eligible state** (после strict или hard прохода).

Если это не так — это ошибка, и мы её фиксируем.

## 📊 Текущее состояние модели

### Что работает хорошо:
1. ✅ **Eligibility gates** предотвращают семантически неправильные классификации
2. ✅ **Двухпроходная схема** снижает количество `all_filtered`
3. ✅ **Final fallback** гарантирует, что всегда будет выбрано какое-то состояние
4. ✅ **Логирование** помогает диагностировать проблемы

### Что ещё нужно улучшить:
1. ⚠️ **`uncertain_state_rate` = 29.57%** (цель: ≤12%)
   - Большая часть всё ещё из-за `all_filtered`
   - Нужно проанализировать логи и ослабить правила
   
2. ⚠️ **`connected` появляется при F_high/T_high/Vneg**
   - Это семантическая ошибка
   - Нужно проверить, не обходится ли eligibility в fallback-пути

3. ⚠️ **Evening flow показывает 0% метрик**
   - Проблема со сбором статистики для вечерних сессий
   - Нужно исправить подсчёт

## 🔍 Как работает классификация сейчас (пошагово)

### Упрощённый пример:

**Входные данные:**
```
Valence = 2 (негативно)
Energy = 1 (очень низко → высокая усталость)
Tension = 9 (очень высоко)
Clarity = 4 (низко-средне)
Control = 1 (очень низко)
Social = 3 (низко)
```

**Шаг 1: Преобразование в вектор**
```
valence = -1.5 (негативно)
fatigue = 2.2 (высокая усталость, т.к. energy < 5)
tension = 2.4 (очень высоко)
agency = 0.0 (очень низко)
certainty = 0.8 (низко-средне)
socialness = 0.5 (низко)
```

**Шаг 2: Вычисление расстояний**
```
exhausted: расстояние = 0.15 (близко!)
overloaded: расстояние = 0.18
blocked: расстояние = 0.20
down: расстояние = 0.22
...
connected: расстояние = 0.45 (далеко)
```

**Шаг 3: Применение eligibility (strict pass)**
```
exhausted: F_high ✅, Ar_low ✅ → ELIGIBLE
overloaded: T_high ✅, F_high ✅, Ag_low ✅, Vneg ✅ → ELIGIBLE
blocked: T_high ✅, Ag_low ✅ → ELIGIBLE
connected: F_high ❌ (блокер!) → NOT ELIGIBLE
```

**Шаг 4: Выбор результата**
- Top-1 по расстоянию: `exhausted` (0.15)
- Top-1 среди eligible: `exhausted` (тоже 0.15)
- **Результат: `exhausted`** ✅

### Пример с проблемой (all_filtered):

**Входные данные:**
```
Valence = 5 (нейтрально)
Energy = 5 (средне)
Tension = 5 (средне)
Clarity = 2 (очень низко)
Control = 5 (средне)
Social = 5 (средне)
```

**Шаг 1: Преобразование в вектор**
```
valence = 0.0 (нейтрально)
arousal = 0.0 (т.к. energy = 5, нет ни усталости, ни активации)
tension = 1.0 (средне)
agency = 1.0 (средне)
certainty = 0.2 (очень низко)
socialness = 1.0 (средне)
```

**Шаг 2: Вычисление расстояний**
```
Все состояния примерно на одинаковом расстоянии (~0.25-0.30)
```

**Шаг 3: Применение eligibility (strict pass)**
```
exhausted: требует F_high → NOT ELIGIBLE (усталости нет)
overloaded: требует T_high И F_high → NOT ELIGIBLE
blocked: требует T_high И Ag_low → NOT ELIGIBLE
connected: требует S_high → NOT ELIGIBLE (социальность средняя)
grounded: требует T_low И Ag_high И C_high → NOT ELIGIBLE
engaged: требует Vpos И Ar_high → NOT ELIGIBLE
...
ВСЕ 11 состояний отфильтрованы! ❌
```

**Шаг 4: Rescue pass (hard)**
```
Проверяем только критические блокеры:
exhausted: нет блокеров → ELIGIBLE ✅
overloaded: нет блокеров (если не проверять требования) → ELIGIBLE ✅
...
```

**Шаг 5: Выбор результата**
- Если hard pass нашёл eligible: выбираем top-1 среди них
- Если hard тоже отфильтровал всё: используем final fallback (top macro state)

## 🎯 Цели и метрики

### Целевые показатели:
- `uncertain_state_rate` ≤ 12% (сейчас 29.57%)
- `all_filtered` → 0% (сейчас 100% от forced uncertain)
- Все состояния достижимы (overloaded, blocked, pressured > 0)
- Семантическая корректность (connected не появляется при F_high/T_high/Vneg)

### Текущие показатели (из golden metrics):
- `uncertain_state_rate`: 29.57% ⚠️
- `forced_uncertain_rate`: 29.57%
- `all_filtered`: 100% от forced uncertain ⚠️
- `needs_refine_rate`: 4.79% ✅
- `low_confidence_rate`: 4.79% ✅
- `overloaded`: 0.19% (60 paths) ✅

## 🔧 Что дальше?

1. **Запустить симуляцию** и посмотреть отчёт с top ineligibility reasons
2. **Проанализировать логи** all_filtered случаев
3. **Ослабить правила**, которые слишком часто блокируют состояния
4. **Исправить проблему с connected** (EPIC S)
5. **Исправить evening flow метрики** (EPIC T)

## 💡 Ключевой вывод

**Модель работает правильно**, но правила eligibility слишком строгие. Это приводит к тому, что многие комбинации метрик не проходят ни одно правило, и система вынуждена возвращать `uncertain`.

**Решение:** Ослабить правила, добавить больше "мягких" состояний, которые могут покрыть средние/нейтральные комбинации метрик.
