# Precision Improvement Analysis: Exhausted vs Overloaded

**Date**: 2024-12-XX  
**Issue**: Неравномерность требований eligibility (2 vs 4-5 условий) может приводить к ложным срабатываниям  
**Goal**: Максимизировать точность классификации при сохранении доступности состояний

## Проблема

### Неравномерность требований

**`exhausted` (строгий режим):**
- Требует: `F_high` (fatigue ≥ 1.2) И `Ar_low` (arousal < 0.35)
- **Всего: 2 требования**

**`overloaded` (строгий режим):**
- Требует: `T_high` (tension ≥ 1.8) И `F_high` (fatigue ≥ 1.2) И `Ag_low` (agency < 0.5) И `Vneg` (valence ≤ -0.8)
- Блокируется: `Vpos` (positive valence) ИЛИ `C_high` (certainty ≥ 1.5)
- **Всего: 4-5 требований**

### Риск ложных срабатываний

Если пользователь почти достиг условий для `overloaded` (4-5 требований), но чуть-чуть не хватило, система может классифицировать как `exhausted` (только 2 требования) из-за:
1. Более простых требований eligibility
2. Лучшего similarity score (близость к центроиду `exhausted`)

**Пример проблемного случая:**
- Baseline: `valence: 3, energy: 1, tension: 6, clarity: 5, control: 1, social: 1`
- Условия: `Vneg: true, F_high: true, T_high: true, Ag_low: true, C_mid: true`
- `overloaded` eligible: ✅ (все условия выполнены)
- `exhausted` eligible: ✅ (F_high + Ar_low выполнены)
- Результат: `exhaustedScore: 0.409` vs `overloadedScore: 0.408` → **exhausted выигрывает!**

## Решение

### Изменение: Блокер для `exhausted` при `T_high`

**Логика:** `exhausted` = усталость без напряжения. Если есть высокое напряжение (`T_high`), это скорее `overloaded`, `blocked` или `pressured`, а не `exhausted`.

**Изменение в `stateEligibility.js`:**
```javascript
case 'exhausted':
  // Strict mode: full rules
  if (!levels.F_high) {
    reasons.push('needs F_high');
    return { eligible: false, reasons };
  }
  if (!levels.Ar_low) {
    reasons.push('needs Ar_low');
    return { eligible: false, reasons };
  }
  // Task: Precision improvement - block exhausted if T_high (overloaded pattern)
  // Exhausted = tired but not tense. If tense, prefer overloaded/blocked/pressured.
  if (levels.T_high) {
    reasons.push('blocked by tension high (likely overloaded)');
    return { eligible: false, reasons };
  }
  return { eligible: true, reasons: [] };
```

## Результаты

### До изменений

**Eligibility:**
- `exhausted` eligible: 33,614 / 117,649 (28.6%)
- `overloaded` eligible: 840 / 117,649 (0.71%)

**Точность для `overloaded`:**
- `overloaded` eligible → classified as `exhausted`: 2 случая (0.24%)
- `overloaded` eligible → classified as `overloaded`: 832 случая (99.05%)
- **Точность: 99.05%** (хорошо, но не идеально)

**Распределение в simplified balance:**
- `exhausted`: 26.25% (8,202 paths)
- `overloaded`: 0.38% (120 paths)

### После изменений

**Eligibility:**
- `exhausted` eligible: 24,010 / 117,649 (20.41%) ⬇️ -8.2%
- `overloaded` eligible: 840 / 117,649 (0.71%) (без изменений)

**Точность для `overloaded`:**
- `overloaded` eligible → classified as `exhausted`: **0 случаев (0.00%)** ✅
- `overloaded` eligible → classified as `overloaded`: 834 случая (99.29%)
- **Точность: 99.29%** (улучшение!)

**Распределение в simplified balance:**
- `exhausted`: **12.76%** (3,986 paths) ⬇️ -51% (уменьшилось вдвое)
- `overloaded`: **2.94%** (920 paths) ⬆️ +667% (увеличилось в 7.7 раз)

## Анализ

### ✅ Улучшения

1. **Точность для `overloaded`:** 99.05% → 99.29% (устранены 2 ложных срабатывания)
2. **Распределение:** `exhausted` уменьшилось с 26.25% до 12.76% (более реалистично)
3. **Распределение:** `overloaded` увеличилось с 0.38% до 2.94% (более реалистично)
4. **Семантическая точность:** `exhausted` теперь не может выиграть при `T_high` (правильно)

### ⚠️ Потенциальные риски

1. **Доступность `exhausted`:** Уменьшилась с 28.6% до 20.41% (-8.2%)
   - **Оценка:** Приемлемо, так как `exhausted` все еще доступен в 20% случаев
   - **Компенсация:** Теперь `exhausted` более точно отражает "усталость без напряжения"

2. **6 случаев `overloaded` eligible не классифицируются как `overloaded`:**
   - **Причина:** Возможно, другие состояния (например, `blocked`, `pressured`) имеют лучший similarity score
   - **Оценка:** Это нормально - система выбирает наиболее близкое состояние по similarity
   - **Рекомендация:** Проверить, что эти случаи классифицируются как семантически близкие состояния

## Рекомендации

### ✅ Принято

1. **Блокер `T_high` для `exhausted`:** Улучшает точность и семантическую корректность
2. **Мониторинг:** Отслеживать распределение состояний в production

### 🔄 Для дальнейшего улучшения (опционально)

1. **Усилить scoring для `overloaded`:**
   - Добавить бонус к similarity score, если выполнены все условия eligibility
   - Это поможет `overloaded` выигрывать в пограничных случаях

2. **Проверить другие состояния:**
   - Анализировать неравномерность требований для других пар состояний
   - Например: `blocked` vs `pressured`, `down` vs `exhausted`

3. **Telemetry:**
   - Собирать данные о случаях, когда пользователь "почти достиг" состояния
   - Анализировать, правильно ли система классифицирует пограничные случаи

## Выводы

1. **Текущая система работает хорошо:** 99.29% точность для `overloaded` после улучшений
2. **Неравномерность требований была проблемой:** Устранена через блокер `T_high` для `exhausted`
3. **Распределение стало более реалистичным:** `exhausted` уменьшилось, `overloaded` увеличилось
4. **Точность важнее доступности:** Лучше иметь меньше, но более точных классификаций

## Метрики для мониторинга

- **Точность для `overloaded`:** Должна оставаться ≥ 99%
- **Распределение `exhausted`:** Должно оставаться в диапазоне 10-15%
- **Распределение `overloaded`:** Должно оставаться в диапазоне 2-4%
- **False positive rate:** Отслеживать случаи, когда пользователь ожидает одно состояние, а получает другое

---

**Status:** ✅ Изменения применены и протестированы. Система готова к использованию.
